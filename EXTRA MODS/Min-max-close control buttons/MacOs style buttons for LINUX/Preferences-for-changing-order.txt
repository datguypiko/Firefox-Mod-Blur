Enable preference of your choice in about:config. (type " about:config " inside searchbar)

Copy paste it and enable:

mod.macos.style.buttons.left.side.default.order

mod.macos.style.buttons.left.side.close-min-max
  
mod.macos.style.buttons.left.side.close-max-min
  
mod.macos.style.buttons.right.side.default.order
  
mod.macos.style.buttons.right.side.min-max-close
 
mod.macos.style.buttons.right.side.max-min-close
